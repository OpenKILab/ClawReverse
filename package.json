{
  "name": "step-rollback",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw native step rollback plugin built from the project docs.",
  "type": "module",
  "main": "./dist/index.js",
  "exports": "./dist/index.js",
  "scripts": {
    "test": "node --test"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "engines": {
    "node": ">=24"
  }
}
